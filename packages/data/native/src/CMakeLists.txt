# Find all headers and sources (no updates to this file necessary!)
FILE(GLOB DATA_NATIVE_HEADERS *.hpp)
FILE(GLOB DATA_NATIVE_SOURCES *.cpp)

IF(CMAKE_VERBOSE_CONFIGURE)
  MESSAGE("DATA_NATIVE_HEADERS = ${DATA_NATIVE_HEADERS}")
  MESSAGE("DATA_NATIVE_SOURCES = ${DATA_NATIVE_SOURCES}")
ENDIF()

SET(SUBPACKAGE_LIB_NAME data_native)

# Create the utilitycore library
ADD_LIBRARY(${SUBPACKAGE_LIB_NAME} ${DATA_NATIVE_SOURCES})
TARGET_LINK_LIBRARIES(${SUBPACKAGE_LIB_NAME} utility_core ${Boost_LIBRARIES})

INSTALL(TARGETS ${SUBPACKAGE_LIB_NAME}
  DESTINATION ${CMAKE_INSTALL_PREFIX}/lib)
INSTALL(FILES ${DATA_ACE_HEADERS}
  DESTINATION ${CMAKE_INSTALL_PREFIX}/include
  PERMISSIONS OWNER_READ GROUP_READ WORLD_READ)
