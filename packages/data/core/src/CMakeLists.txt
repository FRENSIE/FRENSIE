# Find all headers and sources (no updates to this file necessary!)
FILE(GLOB DATA_CORE_HEADERS *.hpp)
FILE(GLOB DATA_CORE_SOURCES *.cpp)

IF(CMAKE_VERBOSE_CONFIGURE)
  MESSAGE("DATA_CORE_HEADERS = ${DATA_CORE_HEADERS}")
  MESSAGE("DATA_CORE_SOURCES = ${DATA_CORE_SOURCES}")
ENDIF()

SET(SUBPACKAGE_LIB_NAME data_core)

# Create the data_core library
ADD_LIBRARY(${SUBPACKAGE_LIB_NAME} ${DATA_CORE_SOURCES})
TARGET_LINK_LIBRARIES(${SUBPACKAGE_LIB_NAME} utility_core )

INSTALL(TARGETS ${SUBPACKAGE_LIB_NAME}
  DESTINATION ${CMAKE_INSTALL_PREFIX}/lib)
INSTALL(FILES ${DATA_CORE_HEADERS}
  DESTINATION ${CMAKE_INSTALL_PREFIX}/include
  PERMISSIONS OWNER_READ GROUP_READ WORLD_READ)
