# Set up the data package directory hierarchy
ADD_SUBDIRECTORY(ace)
INCLUDE_DIRECTORIES(ace/src)

ADD_SUBDIRECTORY(epdl)
INCLUDE_DIRECTORIES(epdl/src)

ADD_SUBDIRECTORY(endf)
INCLUDE_DIRECTORIES(endf/src)

ADD_SUBDIRECTORY(decay)
INCLUDE_DIRECTORIES(decay/src)

ADD_SUBDIRECTORY(native)
INClUDE_DIRECTORIES(native/src)
