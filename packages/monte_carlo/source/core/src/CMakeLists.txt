# Find all headers and sources (no updates to this file necessary!)
FILE(GLOB FRENSIE_SOURCE_CORE_HEADERS *.hpp)
FILE(GLOB FRENSIE_SOURCE_CORE_SOURCES *.cpp)

IF(CMAKE_VERBOSE_CONFIGURE)
  MESSAGE("FRENSIE_SOURCE_CORE_HEADERS = ${FRENSIE_SOURCE_CORE_HEADERS}")
  MESSAGE("FRENSIE_SOURCE_CORE_SOURCES = ${FRENSIE_SOURCE_CORE_SOURCES}")
ENDIF()

NSTALL(FILES ${FRENSIE_SOURCE_CORE_HEADERS}
  DESTINATION ${CMAKE_INSTALL_PREFIX}/include
  PERMISSIONS OWNER_READ GROUP_READ WORLD_READ)
