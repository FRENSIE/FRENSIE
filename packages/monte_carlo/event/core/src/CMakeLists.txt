# Find all headers and sources (no updates to this file necessary!)
FILE(GLOB FRENSIE_ESTIMATOR_CORE_HEADERS *.hpp)
FILE(GLOB FRENSIE_ESTIMATOR_CORE_SOURCES *.cpp)

IF(CMAKE_VERBOSE_CONFIGURE)
  MESSAGE("FRENSIE_ESTIMATOR_CORE_HEADERS = ${FRENSIE_ESTIMATOR_CORE_HEADERS}")
  MESSAGE("FRENSIE_ESTIMATOR_CORE_SOURCES = ${FRENSIE_ESTIMATOR_CORE_SOURCES}")
ENDIF()

INSTALL(FILES ${FRENSIE_ESTIMATOR_CORE_HEADERS}
  DESTINATION ${CMAKE_INSTALL_PREFIX}/include
  PERMISSIONS OWNER_READ GROUP_READ WORLD_READ)
