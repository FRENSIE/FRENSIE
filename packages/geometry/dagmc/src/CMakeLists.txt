FRENSIE_SETUP_PACKAGE(geometry_dagmc
  MPI_LIBRARIES ${MPI_CXX_LIBRARIES} 
  NON_MPI_LIBRARIES ${Boost_LIBRARIES} ${MOAB} ${DAGMC} utility_core utility_archive geometry_core
  SET_VERBOSE ${CMAKE_VERBOSE_CONFIGURE})
