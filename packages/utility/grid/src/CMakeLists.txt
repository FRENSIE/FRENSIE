# Find all headers and sources (no updates to this file necessary!)
FILE(GLOB UTILITY_GRID_HEADERS *.hpp)
FILE(GLOB UTILITY_GRID_SOURCES *.cpp)

IF(CMAKE_VERBOSE_CONFIGURE)
  MESSAGE("UTILITY_GRID_HEADERS = ${UTILITY_GRID_HEADERS}")
  MESSAGE("UTILITY_GRID_SOURCES = ${UTILITY_GRID_SOURCES}")
ENDIF()

INSTALL(FILES ${UTILITY_GRID_HEADERS}
  DESTINATION ${CMAKE_INSTALL_PREFIX}/include
  PERMISSIONS OWNER_READ GROUP_READ WORLD_READ)
