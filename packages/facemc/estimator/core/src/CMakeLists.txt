# Find all headers and sources (no updates to this file necessary!)
FILE(GLOB FACEMC_ESTIMATOR_CORE_HEADERS *.hpp)
FILE(GLOB FACEMC_ESTIMATOR_CORE_SOURCES *.cpp)

IF(CMAKE_VERBOSE_CONFIGURE)
  MESSAGE("FACEMC_ESTIMATOR_CORE_HEADERS = ${FACEMC_ESTIMATOR_CORE_HEADERS}")
  MESSAGE("FACEMC_ESTIMATOR_CORE_SOURCES = ${FACEMC_ESTIMATOR_CORE_SOURCES}")
ENDIF()

INSTALL(FILES ${FACEMC_ESTIMATOR_CORE_HEADERS}
  DESTINATION ${CMAKE_INSTALL_PREFIX}/include
  PERMISSIONS OWNER_READ GROUP_READ WORLD_READ)
