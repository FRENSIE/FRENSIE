# Find all headers and sources (no updates to this file necessary!)
FILE(GLOB FACEMC_SOURCE_CORE_HEADERS *.hpp)
FILE(GLOB FACEMC_SOURCE_CORE_SOURCES *.cpp)

IF(CMAKE_VERBOSE_CONFIGURE)
  MESSAGE("FACEMC_SOURCE_CORE_HEADERS = ${FACEMC_SOURCE_CORE_HEADERS}")
  MESSAGE("FACEMC_SOURCE_CORE_SOURCES = ${FACEMC_SOURCE_CORE_SOURCES}")
ENDIF()

NSTALL(FILES ${FACEMC_SOURCE_CORE_HEADERS}
  DESTINATION ${CMAKE_INSTALL_PREFIX}/include
  PERMISSIONS OWNER_READ GROUP_READ WORLD_READ)
