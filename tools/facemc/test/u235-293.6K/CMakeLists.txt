# Configure files needing absolute paths
CONFIGURE_FILE(${CMAKE_CURRENT_SOURCE_DIR}/dag-geom.xml.in
  ${CMAKE_CURRENT_BINARY_DIR}/dag-geom.xml)

CONFIGURE_FILE(${CMAKE_CURRENT_SOURCE_DIR}/root-geom.xml.in
  ${CMAKE_CURRENT_BINARY_DIR}/root-geom.xml)

# Build the root geometry file
IF(FRENSIE_ENABLE_ROOT)
  EXECUTE_PROCESS(COMMAND ${ROOT_EXE} ${CMAKE_CURRENT_SOURCE_DIR}/u_sphere.c
    WORKING_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR}
    OUTPUT_QUIET
    ERROR_QUIET)
ENDIF()