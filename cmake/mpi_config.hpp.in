// Define if we want to use MPI
// NOTE: If the CMake variable HAVE_${POJECT_NAME}_MPI is defined the macro
//       HAVE_${PROJECT_NAME}_MPI will be set. If the CMake variable
//       does not exists, the macro will be commented out.
#ifndef MPI_CONFIGURE_HPP

${CMAKEDEFINE} HAVE_${PROJECT_NAME}_MPI

#ifdef HAVE_${PROJECT_NAME}_MPI
#include "mpi.h"
#endif

#endif // end MPI_CONFIGURE_HPP
