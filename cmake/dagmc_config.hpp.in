// Define if we want to use DagMC
// NOTE: If the CMake variable HAVE_${PROJECT_NAME}_DAGMC is defined the macro
//       HAVE_${PROJECT_NAME}_DAGMC will be set. If the CMake variable does
//       not exist, the macro will be commented out.
#ifndef DAGMC_CONFIGURE_HPP

${CMAKEDEFINE} HAVE_${PROJECT_NAME}_DAGMC

#ifdef HAVE_${PROJECT_NAME}_DAGMC
// MOAB Includes
#include <DagMC.hpp>
#endif

#endif // end DAGMC_CONFIGURE_HPP
